prefix: at
sources:
  - url: https://github.com/entigolabs/entigo-infralib-release
version: stable
agent_version: latest
steps:
  - name: net
    type: terraform
    approve: never
    modules:
      - name: main
        source: google/vpc
        version: stable